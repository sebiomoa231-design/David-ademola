PORT=4000
MONGODB_URI=mongodb://localhost:27017/david-ai
JWT_SECRET=replace_this_with_a_long_random_string
JWT_EXPIRES_IN=7d
CLIENT_ORIGIN=http://localhost:3000

# Provider API keys - add these when you're ready to go live.
# Until a key is set, that provider's requests fall back to the mock
# generator in src/providers/nativeProvider.js so the app stays usable
# end-to-end during development.
GOOGLE_API_KEY=
OPENAI_API_KEY=
BYTEDANCE_API_KEY=
XAI_API_KEY=
