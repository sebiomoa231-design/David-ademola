# AI CORE SHORT PROMPT

Implement the David AI Core directly into the existing backend. It must centralize intent understanding, context/memory, planning, capability matching, provider/model routing, multi-model orchestration, policy authorization, tool execution, fallback/retry, result validation, conversation persistence, memory learning, and audit/event records.

Do not create a demo. Preserve existing APIs and behavior. Use existing provider/agent/tool registries. Never expose secrets. Test real behavior and fix failures before commit/deploy.
